[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "PluginA"
version = "1.0.0"
description = "A sample plugin for the Tenrec framework."
readme = "README.md"
license = {text = "MIT"}
authors = [{name = "Axel Mierczuk"}]
requires-python = ">=3.10"
dependencies = [
    "tenrec",
]

[tool.uv.sources]
tenrec = { path = "../../", editable = true }
