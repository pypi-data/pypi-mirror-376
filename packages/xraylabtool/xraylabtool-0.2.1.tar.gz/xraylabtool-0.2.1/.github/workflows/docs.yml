name: Documentation

on:
  push:
    branches: [ main, develop ]
    paths:
      - 'docs/**'
      - 'xraylabtool/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - 'pyproject.toml'
      - '.github/workflows/docs.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'docs/**'
      - 'xraylabtool/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - 'pyproject.toml'
      - '.github/workflows/docs.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-docs:
    name: Build Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      matrix:
        python-version: ["3.12"]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: 'pip'
        cache-dependency-path: |
          pyproject.toml
          docs/requirements.txt

    - name: Cache docs dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: docs-deps-${{ runner.os }}-py${{ matrix.python-version }}-${{ hashFiles('pyproject.toml', 'docs/requirements.txt') }}
        restore-keys: |
          docs-deps-${{ runner.os }}-py${{ matrix.python-version }}-

    - name: Install package with docs dependencies
      uses: nick-fields/retry@v3
      with:
        timeout_minutes: 8
        max_attempts: 3
        retry_on: error
        command: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -e .[docs]

    - name: Build Sphinx documentation
      run: |
        sphinx-build -W --keep-going -b html docs docs/_build/html

    - name: Check documentation coverage
      run: |
        sphinx-build -b coverage docs docs/_build/coverage
        echo "Documentation Coverage Report:"
        cat docs/_build/coverage/python.txt || echo "No coverage report generated"

    - name: Upload documentation artifacts
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: documentation-${{ matrix.python-version }}
        path: docs/_build/html/
        retention-days: 30

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: documentation-coverage-${{ matrix.python-version }}
        path: docs/_build/coverage/
        retention-days: 7

  linkcheck:
    name: Check Links
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build-docs
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'
        cache-dependency-path: |
          pyproject.toml
          docs/requirements.txt

    - name: Cache docs dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: docs-deps-${{ runner.os }}-py3.12-${{ hashFiles('pyproject.toml', 'docs/requirements.txt') }}
        restore-keys: |
          docs-deps-${{ runner.os }}-py3.12-

    - name: Install package with docs dependencies
      uses: nick-fields/retry@v3
      with:
        timeout_minutes: 8
        max_attempts: 3
        retry_on: error
        command: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -e .[docs]

    - name: Check external links
      run: |
        sphinx-build -W --keep-going -b linkcheck docs docs/_build/linkcheck
      continue-on-error: true

    - name: Upload linkcheck results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: linkcheck-results
        path: docs/_build/linkcheck/
        retention-days: 7

  doctest:
    name: Test Documentation Examples
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: build-docs
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'
        cache-dependency-path: |
          pyproject.toml
          docs/requirements.txt

    - name: Cache docs dependencies
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: docs-deps-${{ runner.os }}-py3.12-${{ hashFiles('pyproject.toml', 'docs/requirements.txt') }}
        restore-keys: |
          docs-deps-${{ runner.os }}-py3.12-

    - name: Install package with docs dependencies
      uses: nick-fields/retry@v3
      with:
        timeout_minutes: 8
        max_attempts: 3
        retry_on: error
        command: |
          python -m pip install --upgrade pip setuptools wheel
          pip install -e .[docs]

    - name: Test documentation examples
      run: |
        sphinx-build -W --keep-going -b doctest docs docs/_build/doctest

    - name: Upload doctest results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: doctest-results
        path: docs/_build/doctest/
        retention-days: 7

  docs-quality:
    name: Documentation Quality Checks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Set up Python 3.12
      uses: actions/setup-python@v5
      with:
        python-version: "3.12"
        cache: 'pip'

    - name: Cache doc quality tools
      uses: actions/cache@v4
      with:
        path: ~/.cache/pip
        key: doc-quality-deps-${{ runner.os }}-doc8-rstcheck
        restore-keys: |
          doc-quality-deps-${{ runner.os }}-

    - name: Install doc quality tools
      uses: nick-fields/retry@v3
      with:
        timeout_minutes: 5
        max_attempts: 3
        retry_on: error
        command: |
          python -m pip install --upgrade pip
          pip install doc8 rstcheck

    - name: Check RST syntax
      run: |
        find docs -name "*.rst" -exec rstcheck --report-level warning {} \;
      continue-on-error: true

    - name: Check documentation style
      run: |
        doc8 docs --ignore-path docs/_build --max-line-length 100
      continue-on-error: true
