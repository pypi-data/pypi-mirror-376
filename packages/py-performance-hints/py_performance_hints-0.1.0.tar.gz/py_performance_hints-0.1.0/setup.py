#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name="py-performance-hints",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[],
)