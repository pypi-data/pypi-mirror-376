[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "secdefender"
version = "0.1.0"
description = "Lightweight cybersecurity defensive toolkit - log analyzer & IOC matcher"
authors = [{name="Your Name", email="you@example.com"}]
readme = "README.md"
license = {text = "MIT"}
dependencies = []

[project.scripts]
secdefender = "secdefender:main"