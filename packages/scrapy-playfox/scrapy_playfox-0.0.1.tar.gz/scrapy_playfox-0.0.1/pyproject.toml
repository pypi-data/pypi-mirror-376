[project]
name = "scrapy_playfox"
version = "0.0.1"
description = "Yet another camoufox integration for scrapy."
license="Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "camoufox[geoip]>=0.4.11",
    "scrapy-playwright>=0.0.44",
]

[build-system]
requires = ["uv_build>=0.8.13,<0.9.0"]
build-backend = "uv_build"