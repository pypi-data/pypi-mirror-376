[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "emoji_getter"
version = "2025.9.1494"
description = "import re   def emoji_extract(text: str) -> str:     \"\"\"     Return a string that contains only emoji characters from text.     Heuristic c…"
readme = "README.md"
requires-python = ">=3.9"
authors = [
  { name = "Eugene Evstafev", email = "hi@eugene.plus" },
]
license = { file = "LICENSE" }
dependencies = [

]

[project.urls]
Repository = "https://pypi.org/project/emoji_getter/"
