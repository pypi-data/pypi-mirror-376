[project]
name = "brewinglib-generic"
version = "0.0.1-rc5"
description = "Convert a class with generic type parameters to be generic at runtime."
readme = "README.md"
authors = [
    { name = "Mike Cowie", email = "24739590+mikemcowie@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/brewinglib"]
