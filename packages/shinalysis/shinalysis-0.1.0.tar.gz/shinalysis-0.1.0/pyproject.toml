[project]
name = "shinalysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Cole", email = "cmmeyer@arizona.edu" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
shinalysis = "shinalysis:main"

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"
