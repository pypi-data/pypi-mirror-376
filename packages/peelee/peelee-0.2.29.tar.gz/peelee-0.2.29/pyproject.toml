[tool.poetry]
name = "peelee"
version = "0.2.29"
description = "A python library to create countless palettes."
authors = ["Bruce Wen <wenijinew@gmail.com>"]
license = "MIT"
readme = "README.md"
packages = [{include = "peelee"}]

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
