[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "coco-api"
version = "0.0.1"
description = "Minimal Python package template for coco-api"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name = "A S Nandanunni", email = "asnqln@gmail.com" }
]
