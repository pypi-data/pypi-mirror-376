[project]
name = "json-to-clickhouse"
version = "0.1.0020"
description = ""
authors = [
    {name = "Vladimir Muzychenko",email = "vladimir.muzychenko@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.8,<4.0"
dependencies = [
    "clickhouse-connect (>=0.8.15,<0.9.0)"
]

[tool.poetry]
packages = [{include = "json_to_clickhouse", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
