[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "minions-ai"
version = "0.0.1"
description = "Reserved package name."
authors = [{ name="Ali Faraji", email="ali@faraji.info" }]
license = { text = "MIT" }
