[build-system]
requires = [
    "setuptools >= 40.6.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "classic-operations"
version = "0.3.4"
authors = [
    {name = "Sergey Variasov", email = "variasov@gmail.com"},
]
description = "Provides primitives for transactional application logic"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "classic-components~=1.2",
]

[project.urls]
Homepage = "https://github.com/variasov/classic-operations"

[project.optional-dependencies]
dev = [
    "build~=1.2.2.post1",
    "pytest==8.3.4",
    "pytest-cov==6.0.0",
    "twine~=4.0",
]

[tool.setuptools.packages.find]
where = ["sources"]
namespaces = true
