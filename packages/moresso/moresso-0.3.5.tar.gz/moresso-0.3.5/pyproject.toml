[project]
name = "moresso"
version = "0.3.5"
description = "Reusable JWT RS256 validator with header-based decorator for AWS Lambda and Python apps"
readme = "README.md"
authors = [
    { name="chalapathy v", email="chalapathyv57@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.8"

dependencies = [
]

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["more_sso"]
