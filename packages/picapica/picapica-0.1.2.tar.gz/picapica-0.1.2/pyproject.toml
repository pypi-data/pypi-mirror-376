[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "picapica"
version = "0.1.2"
description = "A tiny persistent key–value store built on SQLite."
readme = "README.md"
license = "MIT"
license-files = [
    "LICENSE",
]
authors = [{ name = "Michael Dunne" }]
requires-python = ">=3.9"

[project.urls]
Homepage = "https://github.com/mpdunne/pica"
