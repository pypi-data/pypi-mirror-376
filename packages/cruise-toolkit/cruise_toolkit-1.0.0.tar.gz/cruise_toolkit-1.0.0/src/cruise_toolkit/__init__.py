
"""cruise_toolkit
A unified toolkit for barcode/UMI processing and CR/UR alignment with a single CLI.
This package wraps existing scripts from your workflow.

Subcommands are available via `cruise-toolkit <cmd>`, or each command has its own entry point.

Version is managed in pyproject.toml.
"""
__all__ = ["cli", "dispatch"]
