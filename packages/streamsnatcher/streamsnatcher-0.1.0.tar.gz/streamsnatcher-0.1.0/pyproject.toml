[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "streamsnatcher"
version = "0.1.0"
description = "StreamSnatcher — rip and capture streams with style"
authors = [{ name = "C. Hirschauer", email = "c.hirschauer@gmail.com" }]
license = "MIT"
license-files = ["LICENSE.md"]
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
streamsnatcher = "streamsnatcher.cli:main"

[project.urls]
Homepage = "https://github.com/74Thirsty/streamsnatcher"
