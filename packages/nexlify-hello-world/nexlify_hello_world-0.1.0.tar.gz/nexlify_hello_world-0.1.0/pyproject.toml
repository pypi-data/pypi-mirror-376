[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "nexlify-hello-world"
version = "0.1.0"
description = "Un paquete simple que saluda a los usuarios."
readme = "README.md"
authors = [{name = "nexlify"}]
license = {file = "LICENSE"}
requires-python = ">=3.6"
dependencies = []

[project.urls]
Homepage = "https://github.com/tu_usuario/mi_paquete"