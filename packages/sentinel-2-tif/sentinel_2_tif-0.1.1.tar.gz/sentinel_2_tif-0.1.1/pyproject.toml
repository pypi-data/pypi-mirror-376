[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name        = "sentinel_2_tif"
version     = "0.1.1"
description = "Simplified Sentinel-2 TIF generation and spatial-temporal tools"
authors = [
    {name  = "Chase Kusterer"}
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.9"
dependencies = [
    "pandas",
    "numpy",
    "shapely",
    "rioxarray",
    "pystac-client",
    "planetary-computer",
    "odc-stac",
    "tqdm",
    "rasterio",
    "pyproj"
]

[project.optional-dependencies]
dev = ["black", "pytest"]
