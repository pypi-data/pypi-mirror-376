System hostname = {{ hostname }}
Network interface to configure = vio0
IPv4 address for vio0 = autoconf
{%- if ipv6_address %}
IPv6 address for vio0 = {{ ipv6_address }}
IPv6 prefix for length for vio0 = 64
IPv6 default router = fe80::1%vio0
{%- else %}
IPv6 address for vio0 = none
{%- endif %}
Network interface to configure = done
Start sshd(8) by default = yes
Do you want the X Window System to be started by xenodm(1) = no
Change the default console to com0 = no
Password for root account = {{ root_password }}
Public ssh key for root account = {{ ssh_public_key }}
Setup a user = no
Allow root ssh login = prohibit-password
What timezone are you in = UTC
Which disk is the root disk = sd0
{%- if disk_encryption_passphrase is defined %}
Encrypt the root disk with a (p)assphrase or (k)eydisk = p
New passphrase = {{ disk_encryption_passphrase }}
{%- endif %}
{%- if use_autopartitioning_template %}
URL to autopartitioning template for disklabel = {{ disklabel_server_url }}/disklabel-{{ hostname }}
{%- endif %}
Use (W)hole disk MBR, whole disk (G)PT, (O)penBSD area or (E)dit = W
Use (W)hole disk MBR, whole disk (G)PT or (E)dit = W
Use (A)uto layout, (E)dit auto layout, or create (C)ustom layout = a
Location of sets = http
HTTP proxy URL = none
HTTP Server = {{ install_server_url }}
Server directory = {{ install_server_dir }}
Set name(s) = *
Checksum test for site{{ version_short }}.tgz failed. Continue anyway? = yes
Checksum test for site{{ version_short }}-{{ hostname }}.tgz failed. Continue anyway? = yes
Unverified sets: site{{ version_short }}.tgz site{{ version_short }}-{{ hostname }}.tgz. Continue without verification? = yes
Exit to (S)hell, (H)alt or (R)eboot = r
