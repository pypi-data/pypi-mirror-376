[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "my-app-core"
version = "0.1.0"
description = "支持插件的Python应用"
dependencies = []

[project.scripts]
myapp = "myapp.core:main"