[project]
name = "encfilex"
version = "1.1"
description = "EncFilex v1.1 () - layered file encryption (Base64 -> AES-256-GCM -> XChaCha20-Poly1305)"
authors = [
    { name="AkuzzDev", email="akuzz.developer@gmail.com" }
]
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"

dependencies = [
    "argon2-cffi",
    "cryptography",
    "PyNaCl"
]

[project.scripts]
efx = "EFX.main:main_cli"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
