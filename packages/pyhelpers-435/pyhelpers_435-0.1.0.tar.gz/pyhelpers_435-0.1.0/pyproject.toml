[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyhelpers-435"
version = "0.1.0"
description = "A collection of Pythonic helpers using comprehensions, HOFs, and recursion"
authors = [{ name="Boopathy", email="kboopathyk7@email.com"}]
dependencies = []
requires-python = ">=3.8"
