[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "checkhim"
version = "0.1.0"
description = "Python SDK for phone number verification via checkhim.tech"
authors = [
    { name = "CheckHim Tech", email = "opensource@checkhim.tech" }
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "requests>=2.25.0"
]
