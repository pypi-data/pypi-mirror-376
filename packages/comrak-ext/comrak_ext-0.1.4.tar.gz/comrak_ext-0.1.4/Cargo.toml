[dependencies]
comrak_lib = {package = "comrak", version = "0.41.0", features = ["shortcodes"]}
pyo3 = "0.24.2"

[lib]
name = "comrak"
crate-type = ["cdylib"]

[package]
name = "comrak"
version = "0.1.4"
edition = "2021"
readme = "README.md"

[profile.release]
opt-level = 2
lto = "thin"
codegen-units = 1
panic = "abort"
debug = false
strip = "symbols"
