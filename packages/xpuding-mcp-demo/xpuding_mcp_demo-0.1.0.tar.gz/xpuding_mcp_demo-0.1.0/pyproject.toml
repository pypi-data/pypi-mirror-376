[project]
name = "xpuding-mcp-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Xpud1n9", email = "code14pudding@163.com" }
]
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.14.0",
]

[project.scripts]
xpuding-mcp-demo = "xpuding_mcp_demo:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
