from django.apps import AppConfig

class DjangoDecryptedSearchConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "django_eose"
    verbose_name = "Django Encrypted Object Search Engine"
