[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pyPebble"
version = "0.1.0"
description = "Multi purpose N-body sim"
readme = "README.md" 
authors = [{ name = "Connor Crawford" }]
dependencies = []
requires-python = ">=3.7"

