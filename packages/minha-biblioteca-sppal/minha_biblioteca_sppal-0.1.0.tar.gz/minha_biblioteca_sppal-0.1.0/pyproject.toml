[project]
name = "minha-biblioteca-sppal"
version = "0.1.0"
description = "Uma biblioteca de exemplo em Python"
authors = [{ name = "Teu Nome", email = "teu@email.com" }]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
