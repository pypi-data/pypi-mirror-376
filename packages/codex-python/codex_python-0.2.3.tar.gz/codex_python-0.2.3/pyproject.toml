[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

# PEP 621 metadata for sdist naming; version is sourced dynamically from Cargo.
[project]
name = "codex-python"
requires-python = ">=3.12"
dynamic = ["version"]
