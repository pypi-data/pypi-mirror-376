[project]
name = "trantor"
version = "0.1.0"
description = "Project trantor"
readme = "README.md"
authors = [{ name = "Cody", email = "cody@dkdc.dev" }]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
trantor = "trantor:main"

[build-system]
requires = ["uv_build>=0.8.15,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["ipython>=9.5.0", "ruff>=0.13.0"]
