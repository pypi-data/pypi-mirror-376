# -*- coding: utf-8 -*-

"""
pandascorerestapiforallvideogames

This file was automatically generated by APIMATIC v3.0 (
 https://www.apimatic.io ).
"""
from pandascorerestapiforallvideogames.api_helper import APIHelper
from pandascorerestapiforallvideogames.models.base_match import BaseMatch
from pandascorerestapiforallvideogames.models.base_team import BaseTeam


class BracketStanding(object):

    """Implementation of the 'BracketStanding' model.

    Attributes:
        last_match (BaseMatch): The model property of type BaseMatch.
        rank (int): The model property of type int.
        team (BaseTeam): The model property of type BaseTeam.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "last_match": 'last_match',
        "rank": 'rank',
        "team": 'team'
    }

    def __init__(self,
                 last_match=None,
                 rank=None,
                 team=None):
        """Constructor for the BracketStanding class"""

        # Initialize members of the class
        self.last_match = last_match 
        self.rank = rank 
        self.team = team 

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Creates an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        last_match = BaseMatch.from_dictionary(dictionary.get('last_match')) if dictionary.get('last_match') else None
        rank = dictionary.get("rank") if dictionary.get("rank") else None
        team = BaseTeam.from_dictionary(dictionary.get('team')) if dictionary.get('team') else None
        # Return an object of this model
        return cls(last_match,
                   rank,
                   team)

    @classmethod
    def validate(cls, dictionary):
        """Validates dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(value=dictionary.last_match,
                                           type_callable=lambda value: BaseMatch.validate(value),
                                           is_model_dict=True) \
                and APIHelper.is_valid_type(value=dictionary.rank,
                                            type_callable=lambda value: isinstance(value, int)) \
                and APIHelper.is_valid_type(value=dictionary.team,
                                            type_callable=lambda value: BaseTeam.validate(value),
                                            is_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(value=dictionary.get('last_match'),
                                       type_callable=lambda value: BaseMatch.validate(value),
                                       is_model_dict=True) \
            and APIHelper.is_valid_type(value=dictionary.get('rank'),
                                        type_callable=lambda value: isinstance(value, int)) \
            and APIHelper.is_valid_type(value=dictionary.get('team'),
                                        type_callable=lambda value: BaseTeam.validate(value),
                                        is_model_dict=True)

    def __repr__(self):
        return (f'{self.__class__.__name__}('
                f'last_match={self.last_match!r}, '
                f'rank={self.rank!r}, '
                f'team={self.team!r})')

    def __str__(self):
        return (f'{self.__class__.__name__}('
                f'last_match={self.last_match!s}, '
                f'rank={self.rank!s}, '
                f'team={self.team!s})')
